import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    zIndex: 20
  },
  btnCtnr: {
    display: 'flex',
    flexDirection: 'row',
  }
})
